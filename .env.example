# Copy this file to .env and fill in real values.
# .env is git-ignored — never commit real secrets.

PORT=4000

# We'll fill this in during Step 2 when we connect MongoDB
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/syncslate
